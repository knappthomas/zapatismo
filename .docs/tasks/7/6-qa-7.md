# QA Audit – 7

## 0. Preconditions

- Ticket ID: 7
- QA execution date: 2025-02-25
- Audited artifacts in `.docs/tasks/7/`:
  - Present: 1-requirement-7.md, 2-plan-7.md, 3-backend-7.md, 4-frontend-7.md, 5-testing-7.md
  - Missing (if any): —
- Abort due to missing required docs: NO

---

## 1. Audit Results (Per Checkpoint)

### 1. Document completeness & traceability
- **Status:** OK
- **Reference(s):** `.docs/tasks/7/1-requirement-7.md` (sections 0–10), `.docs/tasks/7/2-plan-7.md` (WBS §2, §9 AC traceability)
- **Finding:** Requirement sheet has structured FR/AC (FR-1–FR-22, AC-1–AC-12), In/Out-of-Scope, and data/validation tables. Plan maps FR/AC to work packages (WP-1–WP-9) and execution sequence.

### 2. Plan exists and maps FR/AC to concrete work packages and touchpoints
- **Status:** OK
- **Reference(s):** `.docs/tasks/7/2-plan-7.md` §2 WBS, §9 Acceptance Criteria Traceability
- **Finding:** Plan lists WP-1–WP-9 with deliverables and dependencies; §9 maps each AC to implementation touchpoints and tests.

### 3. Implementation stays within In-Scope and avoids Out-of-Scope
- **Status:** OK
- **Reference(s):** Requirement §0 In-Scope/Out-of-Scope; Plan §0; backend/frontend/testing logs
- **Finding:** Implementation covers workout CRUD, USER-only access, shoe link and shoe-delete guard, test migration, unit/integration/E2E. No Strava sync, no admin workout UI, no Strava integration changes, no automatic shoe distance aggregation.

### 4. No unplanned refactors beyond the plan
- **Status:** OK
- **Reference(s):** Plan §2 WBS; git status and implementation logs
- **Finding:** Changes are confined to new workout module, shoe delete guard, frontend workouts feature, test migration, and tests. No unrelated refactors identified.

### 5. Each MUST requirement (FR-*) implemented or explicitly not in scope
- **Status:** OK
- **Reference(s):** Requirement §4 FR-1–FR-22; backend controller/service/DTOs; frontend routes, navbar, overview, form; shoes.service.ts remove; test migration; tests
- **Finding:** FR-1–FR-5 (REST list/create/get/update/delete) in WorkoutsController/WorkoutsService. FR-6 (USER-only) via @Roles(Role.USER) + RolesGuard. FR-7 (validation) in DTOs and service (end ≥ start, shoe ownership). FR-8 (shoe belongs to user) in ensureShoeBelongsToUser. FR-9 (Workouts menu USER only) in navbar. FR-10–FR-14 (overview, edit, delete confirmation, Add Workout, shoe selector) in overview and form. FR-15–FR-16 (unit/integration tests) in workouts.service.spec.ts, shoes.service.spec.ts, workouts.integration-spec.ts, shoes.integration-spec.ts. FR-17 (OpenAPI/DTOs) via decorators and response DTOs. FR-18–FR-20 (type enum, steps/distance/location) in CreateWorkoutDto/UpdateWorkoutDto. FR-21 (shoe not deletable when linked) in ShoesService.remove. FR-22 (test data migration) in 00004_workouts_for_thomas.ts.

### 6. Acceptance Criteria (AC-*) verifiable via code/tests/logs
- **Status:** OK
- **Reference(s):** Requirement §8 AC-1–AC-12; Plan §9; backend/frontend/testing logs and spec files
- **Finding:** AC-1–AC-7 covered by frontend flows and E2E smoke/fixtures. AC-8/AC-9 by backend unit and integration tests. AC-10 by unit/integration (shoeId not user’s). AC-11 by shoes.service.spec.ts and shoes.integration-spec.ts (409). AC-12 by test migration file and RUN_TEST_MIGRATIONS usage.

### 7. Backend remains stateless REST
- **Status:** OK
- **Reference(s):** `.docs/system/setup-architecture.md` §4.1; `apps/backend/src/workouts/workouts.service.ts`, `apps/backend/src/shoes/shoes.service.ts`
- **Finding:** No in-memory session or app state; all persistence via Prisma/MySQL. WorkoutsController and ShoesController are thin; logic in services.

### 8. Prisma changes only via Prisma Migrate; migrations exist and match plan
- **Status:** OK
- **Reference(s):** Plan §4; `prisma/schema.prisma`, `prisma/migrations/20260225132127_add_workouts/migration.sql`
- **Finding:** Single migration adds Workout model, WorkoutType enum, User.workouts and Shoe.workouts relations, indexes, FK with ON DELETE RESTRICT for shoeId. No manual SQL; schema matches plan §4.1.

### 9. DB schema changes match plan and do not leak into API directly
- **Status:** OK
- **Reference(s):** Plan §3.2, §5; `apps/backend/src/workouts/dto/workout-response.dto.ts`, controller responses
- **Finding:** API returns WorkoutResponseDto (and list of same). DTOs use explicit @ApiProperty fields; Prisma entities not exposed. userId in response is documented in DTO and is domain-relevant ownership, not raw persistence leak.

### 10. Endpoints match plan: method/path/auth/DTO intent
- **Status:** OK
- **Reference(s):** Plan §3.2; `apps/backend/src/workouts/workouts.controller.ts`, `apps/backend/src/shoes/shoes.controller.ts`
- **Finding:** GET/POST /workouts, GET/PATCH/DELETE /workouts/:id with USER, correct request/response DTOs. DELETE /shoes/:id documents 409 (ApiConflictResponse) when shoe in use; ShoesService.remove throws ConflictException when workout count > 0.

### 11. Validation + error handling follow repo patterns
- **Status:** OK
- **Reference(s):** Plan §3.3; `apps/backend/src/workouts/dto/create-workout.dto.ts`, `update-workout.dto.ts`, `workouts.service.ts` (validateEndAfterStart, ensureShoeBelongsToUser); `shoes.service.spec.ts` / integration specs
- **Finding:** class-validator on DTOs (type enum, IsDateString, steps/distance/location ranges, optional shoeId). Service validates end ≥ start (BadRequestException) and shoe ownership (BadRequestException). Shoe delete → ConflictException. Same NotFoundException/BadRequestException pattern as shoes.

### 12. Backend tests exist as planned and pass (or documented as run)
- **Status:** OK
- **Reference(s):** Plan §8, §2 WP-7/WP-8; `apps/backend/src/workouts/workouts.service.spec.ts`, `apps/backend/src/shoes/shoes.service.spec.ts`, `apps/backend/test/workouts.integration-spec.ts`, `apps/backend/test/shoes.integration-spec.ts`; 3-backend-7.md §7
- **Finding:** Unit tests for WorkoutsService (create/findAll/findOne/update/remove, validation/ownership) and ShoesService remove (409 and success). Integration tests for workout CRUD and shoe delete 409. Log documents run commands; CI runs unit and integration tests.

### 13. Contract changes match plan and consistent with implementation
- **Status:** OK
- **Reference(s):** Plan §5; controller and DTO decorators; OpenAPI generated from NestJS at runtime
- **Finding:** Workout endpoints and DTOs documented with @ApiTags, @ApiProperty, response/error decorators. Shoes DELETE documents 409. No standalone OpenAPI file; spec at /api/docs reflects code.

### 14. Frontend changes match plan: routes/components/services
- **Status:** OK
- **Reference(s):** Plan §6; `apps/frontend/src/app/app.routes.ts`, `navbar.component.ts`, `features/workouts/workouts-overview.component.ts`, `workout-form.component.ts`, `core/services/workouts.service.ts`, `core/models/workout.model.ts`
- **Finding:** Routes workouts, workouts/new, workouts/:id/edit with userGuard. Navbar "Workouts" under auth.hasRole('USER'). WorkoutsOverviewComponent (list, Add Workout, Edit/Delete, delete modal). WorkoutFormComponent (create/edit, shoe selector). WorkoutsService (getList, getOne, create, update, delete). Model types aligned to API.

### 15. Frontend architecture conventions followed (frontend.md)
- **Status:** OK
- **Reference(s):** `.docs/system/frontend.md` §8, §9; features/workouts, core/services, core/models, app.routes.ts, navbar
- **Finding:** Feature under features/workouts; service in core/services; model in core/models; routes with loadComponent and userGuard; navbar role check; Tailwind + daisyUI (btn, table, alert, modal, form-control).

### 16. No business logic duplication in UI
- **Status:** OK
- **Reference(s):** `.docs/system/frontend.md` §9; `workout-form.component.ts`, `workouts.service.ts`
- **Finding:** Form does validation for UX (required, range, end ≥ start); backend remains authoritative. No domain rules duplicated; service delegates to API.

### 17. Error handling UX aligns with existing patterns
- **Status:** OK
- **Reference(s):** 4-frontend-7.md §2, §7; shoes-overview/shoe-form pattern; workouts-overview (error signal, alert), workout-form (formError, errorMessage from HttpErrorResponse)
- **Finding:** Overview shows error alert on list failure; form shows formError from API (e.g. 400 body). Same approach as shoes.

### 18. Cypress spec structure matches cypress-testing.mdc (e.g. describe blocks, selectors, intercepts)
- **Status:** NOK
- **Reference(s):** `.cursor/rules/cypress-testing.mdc` (“Every E2E spec file **must** use this structure (exact block names): 1. **Component Test** (top-level describe…) 2. **e2e** (top-level describe…) with fixtures and smoke”); `apps/frontend/cypress/e2e/workouts/workouts.cy.ts`
- **Finding:** Convention requires a top-level `describe('Component Test', ...)` block in addition to `describe('e2e', ...)`. Workouts spec has only `describe('Workouts')` → `describe('e2e')` → fixtures / Error Handling / smoke. No “Component Test” block. Shoes spec has both Component Test and e2e.
- **Deviation (NOK only):** workouts.cy.ts omits the required top-level “Component Test” describe block required by cypress-testing.mdc.
- **Severity (NOK only):** low

### 19. Fixtures organization follows conventions
- **Status:** OK
- **Reference(s):** `.cursor/rules/cypress-testing.mdc` (fixtures in subfolders by feature); `apps/frontend/cypress/fixtures/workouts/` (empty.json, loaded.json, error-400.json, error-500.json)
- **Finding:** Fixtures under cypress/fixtures/workouts/ with state-based names; used in e2e/fixtures and Error Handling tests.

### 20. Smoke test rule respected (e.g. “exactly one smoke test”)
- **Status:** NOK
- **Reference(s):** `.cursor/rules/cypress-testing.mdc` (“**One smoke test only** in the whole repo – a single smoke test that verifies a real user flow”); `apps/frontend/cypress/e2e/shoes/shoes.cy.ts`, `apps/frontend/cypress/e2e/workouts/workouts.cy.ts`
- **Finding:** Convention states one smoke test only in the whole repo. Shoes spec has a smoke describe with real-backend test(s); Workouts spec has a smoke describe with full CRUD smoke test. Plan §9 designates the workout CRUD smoke as the required smoke for this ticket but does not remove or consolidate the shoes smoke; repo therefore has more than one smoke test.
- **Deviation (NOK only):** Two (or more) feature specs contain smoke tests; rule requires a single smoke test in the repo.
- **Severity (NOK only):** low

### 21. CI workflow matches plan (if plan includes it)
- **Status:** OK
- **Reference(s):** Plan §10 (“Run Cypress smoke E2E”); `.github/workflows/system-tests.yml`; 5-testing-7.md §10
- **Finding:** Plan expects backend unit/integration and Cypress E2E. CI runs backend unit and integration tests (with migrate + test-migrations), then Cypress E2E (backend + frontend started, test-migrations applied). Workflow runs `npm run e2e:headless -w frontend`; frontend package has e2e:headless script.

### 22. Repo build/test capability not obviously violated
- **Status:** OK
- **Reference(s):** Backend and frontend package.json scripts; 3-backend-7.md §7, 5-testing-7.md §8; `.github/workflows/system-tests.yml`
- **Finding:** Backend test and test:integration; frontend e2e, e2e:run, e2e:headless. CI runs backend tests and Cypress. No evidence of broken scripts or missing deps for this ticket.

---

## 2. Deviations Overview

| # | Checkpoint (short) | Status | Severity |
|---|---------------------|--------|----------|
| 18 | Cypress spec structure (Component Test block) | NOK | low |
| 20 | One smoke test only in repo | NOK | low |

---

## 3. Overall Assessment

- Count OK: 20
- Count NOK (critical): 0
- Count NOK (medium): 0
- Count NOK (low): 2
- Count Not verifiable: 0

**Acceptable to merge/release:** YES

Rule: Acceptable = YES only if there are no NOK with severity critical and required docs were present. No critical or medium NOK; two low-severity deviations (Cypress structure and smoke-count) do not block merge.

---

## 4. Key References (Optional)

- Requirement: `.docs/tasks/7/1-requirement-7.md` (§0 In/Out-of-Scope, §4 FR, §8 AC)
- Plan: `.docs/tasks/7/2-plan-7.md` (§2 WBS, §3 Backend, §4 Prisma, §6 Frontend, §8 Testing, §9 AC traceability)
- Backend: `apps/backend/src/workouts/*`, `apps/backend/src/shoes/shoes.service.ts`, `apps/backend/src/shoes/shoes.controller.ts`
- Frontend: `apps/frontend/src/app/app.routes.ts`, `apps/frontend/src/app/layout/navbar.component.ts`, `apps/frontend/src/app/features/workouts/*`, `apps/frontend/src/app/core/services/workouts.service.ts`
- Prisma: `prisma/schema.prisma`, `prisma/migrations/20260225132127_add_workouts/migration.sql`, `prisma/test-migrations/00004_workouts_for_thomas.ts`
- Cypress: `apps/frontend/cypress/e2e/workouts/workouts.cy.ts`, `.cursor/rules/cypress-testing.mdc`
- Architecture: `.docs/system/setup-architecture.md`, `.docs/system/frontend.md`
